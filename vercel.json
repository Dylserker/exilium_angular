{
  "version": 2,
  "buildCommand": "pwd && ls -la && find . -name 'frontend' -type d && cd frontend && pwd && npm install && npm run build",
  "outputDirectory": "frontend/dist/frontend/browser",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
} 